import { Component } from '@angular/core';
import { NbLoginComponent } from '@nebular/auth';

@Component({
  selector: 'nts-login',
  templateUrl: './login.component.html',
})
export class NtsLoginComponent extends NbLoginComponent {
}